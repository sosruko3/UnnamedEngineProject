GOAL: CreatingHigh-performance 3D Game engine, similar to ID tech 7, but with the scale of a solo developer. Would be using all the great libraries whenever I find, except renderer part which would be custom GPU-driven using NRI library, with Mesh shaders, bindless textures, 64-bit pointers and more, which might take most of the project's timeline.
Core codebase was from mine old 2D unfinished engine project, deleted 2D systems, replacing lots of parts, like Raylib-> SDL3, and so on. And planning to make it more C++ while still using DOD architecture and Orthodox C++20 rules, codebase was originally C11 and C11 in C++ compiler, and now it feels more C++ code than before finally, while still not forgetting any rule at all.
Currently planned to integrate libraries(Not %100 confirmed): Jolt physics,ozz-animation,dear imgui,spdlog,miniaudio,SDL3,NRI,EnkiTS,cr.h and more.
